# Example Streamlit secrets — copy to .streamlit/secrets.toml (not committed)

approver_password = "change-me"

# Phase 1b — conversational intake (Anthropic)
# ANTHROPIC_API_KEY = "..."
# USE_CHAT_INTAKE = false   # set true when chat UI replaces form

# Phase 2: email sending when staff approves a referral
# WRC_FROM_EMAIL = "wrc-referrals@ccsf.edu"
# SMTP_HOST = "smtp.example.com"
# SMTP_PORT = "587"
# SMTP_USERNAME = "..."
# SMTP_PASSWORD = "..."
